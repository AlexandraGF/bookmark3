<h1> Links </h1>
<h2>Welcome, <%= @user_email %></h2>

<ul id='links'>
    <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL:   <%= link.url   %>
      Tag:   <%= link.tags.map(&:name)  %>
    </li>
  <% end %>
</ul>
